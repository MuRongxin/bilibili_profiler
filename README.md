# B站弹幕发送者用户画像分析系统

输入视频BV号，深度分析发送弹幕的用户都是什么样的人。

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)

这是一个开源的 B站弹幕数据分析工具，采用 MIT 许可证。**使用前请务必阅读文末[免责声明](#免责声明)。**

## 核心特性

- **弹幕采集**：实时弹幕池 + 全量历史弹幕（逐日弹幕池快照，每日上限 1000 条，热门期可能不完整），按发送者聚合
- **mid_hash 破解**：MITM（中间相遇）CRC32 秒级反查，覆盖全部 ≤10 位 UID（2022 年 4 月前注册用户），内存小表无需构建大文件
- **多源明文交叉验证**：评论区（含子评论补采，最多 100 页）+ 充电鸣谢名单 + 互动弹幕 commandDms 获取明文 UID，计算 CRC32 进行匹配；碰撞自动消歧（候选交集 + API 存在性验证）
- **全局映射库**：可靠解析结果跨视频沉淀复用，解析率随使用次数累积
- **IP 属地画像**：从评论接口提取发送者地域分布
- **刷屏智能检测**：不删除、只标记，区分正常重复与恶意刷屏
- **四维度画像分析**：
  1. 用户主页信息（等级、大会员、硬币数、认证等）
  2. 互动内容足迹（投稿、动态、收藏夹、评论）
  3. 社交关系网络（关注列表、粉丝、互关）
  4. 行为模式分析（活跃时段、活跃周期、消费行为）
- **扫码登录**：B站APP扫码，Cookie自动保存复用（支持自动刷新）
- **交互式Web报告**：`python web.py` 启动本地服务（127.0.0.1:8000），首页视频列表（搜索/排序/分页）+ 八标签页报告（概览/用户画像/弹幕浏览器/问题弹幕榜/争执焦点/问题评论榜/高回复评论/低置信度，标签页与筛选状态写入URL可分享，@media print 打印样式）；首页与报告页支持一键删除报告/重新生成；run.py/quick_test.py 分析完毕自动启动并打开（WEB_AUTOSTART 可关）
- **手动弹幕分析**：Web 弹幕浏览器勾选发送者 → 后台强制分析（UID解析+采集+画像+LLM深掘），进度轮询，失败明细透出可重试，完成自动刷新并恢复现场（标签页/筛选/滚动位置）
- **跨视频足迹**：用户卡片「其他视频足迹」区块展示该用户在其他已分析视频中的出现情况，及他在那些视频里发过的弹幕与评论样本（评论随分析持久化到 comments 表）
- **跨视频重叠用户面板**：首页列出在 ≥2 个已分析视频中都出现过的发送者（点视频条目可展开该用户在其中的弹幕/评论明细样本），用于找跨视频带节奏/水军账号。注意覆盖口径：面板数据源是 senders 表，收录的是**通过兴趣分阈值（中/高刷屏、问题弹幕命中）被解析的用户 + 问题评论直引作者**，不要求画像采集成功（名字缺失时显示 UID）；从未命中阈值的发送者不会被解析，也就不在面板范围内——低强度但跨视频持续出现的潜水账号覆盖不到，这是兴趣分漏斗的固有取舍（弹幕匿名，必须逐视频破解才知道是谁）
- **高回复评论页**：回复数达阈值的评论单独成页（潜在争执热点），按回复数降序展示，附高赞回复样本与「去B站围观」链接
- **用户互动时间线**：`/user/<UID>` 页追踪某个用户在全部已分析视频中的弹幕/评论互动（按最近互动倒序，含样本时间；仅覆盖已分析视频范围）
- **数据导出**：CSV/JSON 导出（report_{BV号}_{时间} 前缀），Web 报告页提供下载链接
- **断点续采**：SQLite持久化，中断后可恢复

## 安装

```bash
cd bilibili_profiler
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r requirements.txt
```

首次使用需复制配置模板并按需修改（如需 LLM 分析请填入 `LLM_API_KEY`，或用环境变量覆盖）：

```bash
cp config.example.py src/config.py
```

> `src/config.py` 已在 `.gitignore` 中，请勿提交到任何公共仓库。

## 用法

```bash
# 分析视频
python run.py BV1vu4y1b7Y9

# 强制重新分析（清除该视频全部缓存并强制重采全部用户）
python run.py BV1vu4y1b7Y9 --force

# 限制最大分析用户数
python run.py BV1vu4y1b7Y9 --max-users 50

# 批量分析（逐行读取BV号，忽略空行与 # 注释行）
python run.py --batch videos.txt
```

首次运行会提示扫码登录，请使用B站APP扫描终端显示的二维码。

可选小号池（多号并行提速 + 风控时轮换兜底）：`python login.py alt1` 扫码登录小号（存 `data/cookies/alt1.json`），`run.py` 建组合池时自动发现并入池；用户深度采集阶段按账号分片并行（限速按号独立，N 号≈N 倍吞吐），触发风控时换号（+换IP）重试，触发风控的号不剔除、保留在轮询池。

可选 IP 池（风控时换"新号+新IP"继续采集，长冷却仅作最后手段；不配也能正常运行）：
- 已有运行中的 Clash/ShellCrash：程序自动探测本机控制器（9090/9999/9097），零配置直接用；ShellCrash 建议「模式设置 → 流量劫持范围 → 4 纯净模式」，其它应用流量不受影响。
- 没有梯子工具：在 config.py 或环境变量填 `SUB_URLS`（支持多个机场订阅，逗号分隔），程序自动下载/拉起内置 mihomo 核心（只监听 127.0.0.1 随机端口，不影响其它应用）。

## 断点续采机制

全阶段断点续采：任意位置中断（Ctrl+C/崩溃）后重跑自动续上，无需人工干预——

- **弹幕**：实时池先落库，历史快照逐日增量落库 + 逐日检查点（`phase_state` 表）；中断后重跑从下一个日期续采。
- **评论**：逐页落库 + 游标检查点；中断后重跑从断点页续翻，已入库评论靠 UNIQUE 约束去重。
- **LLM 判定**：批次级缓存（llm_cache），中断后重跑已完成批次零调用。
- **解析/采集**：senders 逐条落库、users 每人落库，重跑自动跳过已完成的解析与采集。
- 重跑时各阶段按库内数据存在性独立跳过（弹幕/评论完整判据：检查点 done=1，或本功能前的旧版完整落库）。
- 同一 UID 被多个弹幕 hash 命中时只采集一次；历史解析失败的发送者会在重跑时自动重试。
- `--force` 会清除该视频的 senders/videos/danmaku/comments/检查点缓存、删除不再被其他视频引用的孤立 users 记录，强制全量重采（拿到最新数据）。

## 输出

- **Web 报告**：`python web.py` 后访问 http://127.0.0.1:8000；后台运行时用 `python web.py --stop` 停止并释放端口
- **数据导出**：`data/reports/report_{BV号}_{时间}.csv` / `.json`（与分析运行同时间戳）
- **数据库**：`data/profiler.db`（支持中断恢复）
- **Cookie**：`data/cookie.json`（主号登录态）+ `data/cookies/*.json`（可选小号池，自动管理）

## 技术架构

```
web.py                 # 交互式 Web 报告服务（Flask，127.0.0.1:8000；CSS/JS 在 static/）
static/                # Web 报告静态资源（report.css/report.js/index.css/index.js）
src/
├── main.py              # 主控流程（6阶段流水线）
├── web_autostart.py     # 分析完毕自动启动Web报告
├── auth.py              # 扫码登录 + Cookie管理
├── api_client.py        # HTTP请求封装（限速、重试）
├── danmaku.py           # 弹幕XML解析 + 发送者聚合
├── comment.py           # 评论区采集 + UID映射
├── uid_resolver.py      # mid_hash破解 + 交叉验证引擎
├── spam_detector.py     # 刷屏智能检测
├── user_collector.py    # 用户深度数据采集（四维度）
├── profile_analyzer.py  # 画像分析 + 标签生成
├── report.py            # 报告渲染函数库（被 web.py 复用）
├── storage.py           # SQLite持久化
└── config.py            # 配置常量
```

## mid_hash 解析策略

| 方法 | 适用场景 | 可靠性 |
|------|----------|--------|
| 评论区交叉验证 | 所有在评论区出现过UID的用户 | ⭐⭐⭐⭐⭐ |
| 充电名单/互动弹幕明文验证 | 充电用户、UP主互动弹幕 | ⭐⭐⭐⭐⭐ |
| MITM 反查 + 碰撞消歧 | 全部 ≤10 位 UID（约2022年4月前注册） | ⭐⭐⭐ |
| API存在性验证 | 所有破解结果 | ⭐⭐⭐⭐ |

注：2022 年后注册的 16 位随机长 UID 数学上无法反推，只能依赖明文源命中。

## 刷屏判定标准

- **高风险**：大量重复内容 + 机器人式规律间隔
- **中风险**：中度重复或疑似机器人
- **低风险**：正常发言

系统**不删除**任何弹幕数据，仅在画像中标记风险等级。

## 注意事项

- B站API有风控限制，请求间隔为基础 0.8–1.6 秒（高风险操作 2–4 秒），区间内随机取时长
- 大会员状态、收藏夹等部分数据需要登录后才能获取完整信息
- 用户关注列表等API可能需要登录态，未登录时可能返回有限数据
- 分析大量用户时耗时较长，程序支持Ctrl+C中断并恢复

## 局限性与已知边界

> 本节说明本工具**在设计与数据源上无法突破的边界**。请把分析结论当作**线索**而非证据；判定口径、抽样范围与误差来源都写在这里，避免误用。

### 1. 数据源边界（B站接口语义决定，改代码也突破不了）

- **实时弹幕池不全**：接口只保留最近若干条，更早的弹幕已被顶出池子。
- **历史弹幕 ≠ 全量**：历史接口返回的是"**截至该日的最新 1000 条弹幕池快照**"，不是"当日发送的弹幕"。相邻日快照大量重叠或整体滚动，**热门视频早期的弹幕会被永久丢掉**，逐日遍历只能"逼近"而无法还原。
- **回溯窗口有上限**：最多 24 个月 / 400 天（`HISTORY_MAX_MONTHS` / `HISTORY_MAX_DAYS`），更早的弹幕不在分析范围内；到了天数上限会写 `truncated` 标记（属设计性截断，不是失败）。
- **多分P视频只采第 1P**：历史弹幕与互动弹幕都固定取第一个分P。
- **评论采不完**：主评论最多 100 页（≈2000 条），每条主评论的楼中楼最多补采 25 页（500 条）；触顶时写 `truncated=1` 而不写 `done`，之后每次重跑都是 0 请求、也不会推进——想采更多需调大 `MAX_COMMENT_PAGES`。
- **问题评论判定会截断**：去重后按点赞降序**只判前 2000 条**（`COMMENT_CRINGE_MAX_ITEMS`），低赞评论不参与判定。
- **用户维度全是采样**：投稿 3 页、动态 5 页、关注 5 页（**他人关注列表接口本身只看得到前 100 人**）、粉丝 2 页、收藏夹内容 20 条。因此"社交关系/兴趣画像"是样本而非全貌。
- **只见当下仍存在的数据**：删稿、删评、隐私设置、已注销账号都拿不到；IP 属地只在评论里（B站已下线主页属地字段）。
- 报告页顶部的"数据边界"提示条与"已解析发送者 / 全部发送者"比例，就是对这些边界的如实标注。

### 2. 匿名身份解析（mid_hash → UID）的数学上限

- MITM CRC32 反查**只覆盖 ≤10 位 UID**（约 2022 年 4 月前注册）；2022 年后的 16 位随机长 UID **数学上不可解**。
- ≤10 位空间下平均约 2.3 个候选/哈希，必须靠明文来源（评论 / 充电名单 / 互动弹幕 / UP主·staff·简介@）或名片接口存在性验证消歧；**多个候选同时存在时只能取最小 UID 并标注"可能误识别"**（低置信度标签页集中列出这些待复核用户）。
- 结论：**新注册账号若从未在评论区或充电名单露面，基本解析不出身份**，只会留在"未解析"里。
- **全流程是兴趣分漏斗**：只有中/高刷屏、问题弹幕命中者，以及达阈值的问题评论作者会进入解析与画像名单，其余发送者不解析（跨视频重叠面板同样只覆盖这部分人）。低强度但长期潜水的账号覆盖不到，这是弹幕匿名性的固有代价。

### 3. 判定准确性（启发式 + LLM 双重不确定）

- **刷屏检测是规则启发式**（重复率、相似度、滑动窗口爆发、全池 P95 相对离群、群体复读事件），阈值可调但**必然有误判**；它只标记 `spam_level`，**不删除任何弹幕**。
- **LLM 八类判定受厂商与模型漂移影响**：判定口径带版本号、缓存按模型名隔离；模型可能返回空正文（会自动重试）、被厂商内容审核拦截（该批按空跳过）。
- 口径是**"宁漏勿冤"**，所以**漏报多于误报**；误报可用条目上的「误报」按钮人工纠正（弹幕按**内容全文**粒度——一条标记会连带所有发送同内容的发送者；评论按 rpid）。
- **AI 深度画像只覆盖兴趣分 top K**（默认 20 人，`LLM_DEEP_TOP_K`），其余用户只有规则标签。

### 4. 规模、风控与网络

- 限速是硬约束：请求间隔基础 0.8–1.6 秒、高风险接口 2–4 秒（区间内随机）。**大视频分析是小时级**——实测 560 人规模时，仅用户采集阶段就耗时约 84 分钟。
- 采集人数上限 = max(300, 独立发送者数×5%)，绝对封顶 1000；但**问题评论作者直引不受该上限约束**，名单可能明显超出。
- `--max-users` 是**按弹幕数降序截断**，而问题评论直引作者的 `danmaku_count=0`，会**最先被砍掉**：想保留这条线就不要用 `--max-users`，或给足够大的值。
- **IP 维度是全局单点**：内置核心只有一个 mixed-port、一个 select 组，所有账号共享同一出口 IP，"换号+换 IP"实际只有换号这一维。
- 依赖有效登录 Cookie；cookie 中若没有 `_refresh_token`，过期后只能重新扫码。
- 触发风控会进入冷却（默认 600 秒，单账号池 120 秒），期间采集停滞；代理节点质量直接影响成功率。

### 5. 平台与部署

- Web 报告**只监听 127.0.0.1** 并强校验 Host/Origin，**不能远程访问**（有意设计）；job 状态与页面缓存在内存里，**服务重启即失**。
- Linux 之外能力打折：`--stop` 在 Windows 依赖 PowerShell 查询进程；防孤儿进程的 `PDEATHSIG` **仅在 Linux 生效**，macOS/Windows 下强杀父进程可能留下 mihomo 孤儿进程。
- 数据是本地单文件 SQLite；库变大后报告渲染变慢。导出文件只增不删，需要自行清理 `data/reports/`。

### 6. 隐私与合规

- **LLM 判定与深掘会把弹幕原文、评论原文、昵称/签名/关注摘要等发送给第三方模型厂商**（DeepSeek / 智谱 / MiMo），属真实的数据出境；不配置 `LLM_API_KEY` 时所有 LLM 功能自动跳过，可完全离线运行。
- Cookie 与小号池等同账号登录态（`data/cookie.json`、`data/cookies/*.json`，已在 `.gitignore` 中）；仍建议 `chmod 600` 并加密存储。
- 对他人弹幕/评论做身份画像是个人信息处理行为，**合规责任完全在使用者**，详见下方[免责声明](#免责声明)。

### 7. 工程现状

- 项目**没有单元测试框架**，端到端验证依赖真实网络与有效 Cookie（`quick_test.py` 冒烟 / `run.py` 全流程）。
- 现有离线检查手段：**`python tests/run_all.py`**（34 项离线回归，秒级、无需网络与 Cookie）+ `--lint` 附带 `pyflakes` 静态检查（抓未定义名这类回归）。改动后建议再跑一次真实冒烟（`quick_test.py` 或 `run.py`）。

## 免责声明

> **请务必在使用本项目前完整阅读本条款。使用、复制、修改或分发本项目代码，即视为您已理解并同意以下全部内容。**

### 1. 用途限制

本项目**仅供个人学习、技术研究与学术交流**，目的是演示弹幕协议分析、哈希反查与数据可视化技术。禁止将本项目用于任何商业用途、盈利活动或大规模自动化数据采集。

### 2. 平台协议与法律法规

使用者必须自行遵守《bilibili 弹幕网用户协议》《哔哩哔哩弹幕网社区公约》及相关法律法规（包括但不限于《网络安全法》《数据安全法》《个人信息保护法》《反不正当竞争法》等）。

本项目与 bilibili（B站）无任何隶属、授权或合作关系。使用本项目访问 B站接口、采集数据的行为，可能违反 B站用户协议或触发风控。使用者需自行评估并承担由此产生的一切后果。

### 3. 账号与数据安全

- 本项目需要使用者提供 B站登录 Cookie。Cookie 等同于账号登录态，**泄露即等于账号被盗**。请务必妥善保管本地 `data/` 目录下的所有文件，避免上传至公共仓库或分享给他人。
- 频繁或不当使用本项目可能导致账号被 B站限制、封禁或追究法律责任。作者不对此承担任何责任。
- 本地数据库（`data/profiler.db`）、导出报告及缓存文件中包含用户画像、弹幕内容、IP属地等敏感信息。使用者应加密存储、及时清理，并在共享或丢弃设备前彻底删除。

### 4. 隐私与个人信息保护

本项目涉及对第三方用户的画像分析，包括但不限于：昵称、头像、等级、关注关系、投稿内容、评论记录、活跃时段、IP 属地等。

- **严禁**将分析结果用于：人肉搜索（开盒）、网络暴力、骚扰、跟踪、歧视、敲诈勒索、商业推销、舆论操纵或其他侵犯他人合法权益的行为。
- **严禁**将分析结果公开传播、售卖、共享或用于训练数据。
- 若被分析用户或 B站平台要求删除数据，使用者应立即无条件删除并停止相关行为。
- 涉及未成年人的内容应受到更高标准的保护，禁止对未成年人进行画像、标记或公开讨论。

### 5. 技术准确性免责

- mid_hash 反查、交叉验证及 LLM 判定均存在误差可能。本项目不保证任何分析结果的准确性、完整性或时效性。
- 画像标签、风险等级、问题弹幕/评论判定均为算法推测，**不构成对任何用户的事实定性**。使用者不得据此对具体用户进行公开指责、挂人、举报围攻或传播不实信息。
- 因算法误差、数据缺失或接口变更导致的任何直接或间接损失，作者不承担赔偿责任。

### 6. LLM 分析相关

- 本项目可选接入第三方大语言模型（如 DeepSeek、GLM、MiMo 等）。使用者需自行承担 API 调用费用、数据出境风险及内容合规风险。
- 请勿将包含真实个人信息、Cookie、UID 或敏感弹幕内容的请求发送给第三方 LLM，除非您已确认其隐私政策并接受相关风险。

### 7. 风险自担与责任豁免

- 本项目按“现状”提供，不提供任何明示或默示的担保。
- 使用者因使用本项目产生的任何法律责任、经济损失、账号损失、名誉损害或第三方索赔，均由使用者自行承担。作者及其贡献者不承担任何连带责任。
- 若 B站平台或监管机构要求下架、删除或限制本项目，作者保留随时修改、暂停或终止项目的权利，且无需另行通知。

### 8. 接受条款

如不同意上述任何条款，请立即停止使用并删除本项目全部代码与数据。继续使用即视为您已充分理解并接受全部风险。

## 测试

项目没有单元测试框架，验证分三层，从便宜到贵：

```bash
# 1) 离线回归：34 项检查，秒级完成，不联网、不用 Cookie、不消耗 LLM 额度（隔离临时库，不碰 data/profiler.db）
python tests/run_all.py            # 跑全部离线回归并汇总
python tests/run_all.py --lint     # 附带 pyflakes 静态检查（需先 pip install pyflakes）
python tests/offline/regress_core.py   # 也可单独跑某一个脚本

# 2) 静态检查：抓未定义名/语法类回归（本项目历史上踩过 NameError）
pip install pyflakes
python -m pyflakes src/*.py web.py run.py quick_test.py login.py

# 3) 真实端到端冒烟（需要有效 Cookie 与网络，会真实请求 B站 API）
python quick_test.py [BV号] [--top N]   # 采样冒烟：刷屏 top N 用户，最快
python run.py <BV号>                     # 完整流水线
```

离线回归覆盖：

| 脚本 | 项数 | 覆盖 |
|---|---|---|
| `tests/offline/regress_core.py` | 14 | 历史弹幕断点续采（中断后必须补齐、失败日挂账、不误写 done）、线程池收尾、`post()` 重试请求体、限速预算熔断、画像渲染容错等 |
| `tests/offline/regress_comment_path.py` | 10 | wbi/legacy 翻页与降级、请求异常保留已采、真重复页检测、缺 rpid 脏行、刷新计数 |
| `tests/offline/regress_judge_danmaku.py` | 4 | 问题弹幕判定必须被采纳并归到正确发送者（防"解析了却没采纳"） |
| `tests/offline/regress_judge_comment.py` | 3 | 问题评论判定必须回映到正确 rpid |
| `tests/offline/regress_config_template.py` | 3 | `config.example.py` 与 `src/config.py` 常量同步（模板漏项会让全新克隆 ImportError） |

`tests/run_all.py` 会自动挑选带依赖的解释器（当前解释器 → 仓库 `.venv`），从任意目录运行均可，失败时退出码非 0，可直接接 CI。

## 贡献

欢迎提交 Issue 和 Pull Request。提交前请：

1. 确保代码风格与现有项目一致（中文注释、中文输出）。
2. 不要提交 `src/config.py`、`data/` 目录或任何含密钥/凭证的文件。
3. 新增 API 调用必须走 `BiliAPIClient`，遵守限速与降级约定。

## 许可证

本项目代码采用 [MIT License](LICENSE) 开源。

**重要**：MIT 许可证仅涉及代码版权授权，**不授权、不豁免**任何数据合规、隐私保护或平台协议义务。使用本项目即视为同意上方[免责声明](#免责声明)的全部条款。

---

如果这个项目对你有帮助，欢迎 Star 或 Fork。